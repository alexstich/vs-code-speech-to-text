---
description: Watch this rule if you want to make or run tests
globs: 
alwaysApply: false
---
# Testing Guide для VS Code Speech-to-Text Extension

## 📁 Структура тестов

Тесты организованы в директории:
- `src/test/unit/` - Unit тесты для отдельных модулей  
- `src/test/integration/` - Integration тесты для полного workflow
- `src/test/mocks/` - Моки для внешних API
- `src/test/fixtures/` - Тестовые данные и fixtures

## 🔧 Конфигурация тестирования

### Основные файлы конфигурации:
- [.mocharc.json](mdc:.mocharc.json) - Настройки Mocha (TDD UI, timeout, setup)
- [package.json](mdc:package.json) - Скрипты тестирования и nyc конфигурация
- [src/test/setup.ts](mdc:src/test/setup.ts) - Глобальная настройка тестовой среды

### Доступные скрипты:
```bash
npm run test:unit        # Unit тесты
npm run test:integration # Integration тесты  
npm run test:coverage    # Тесты с покрытием кода
npm run test:watch       # Тесты в watch режиме
npm run coverage:check   # Проверка покрытия (80% порог)
```

## 🎭 Моки и заглушки

### Web Audio API моки - [webAudioMocks.ts](mdc:src/test/mocks/webAudioMocks.ts):
- `MockMediaRecorder` - для записи аудио
- `MockMediaStream` - для медиа потоков
- `MockBlob`, `MockFormData` - для данных
- `setupWebAudioMocks()` / `cleanupWebAudioMocks()` - настройка/очистка

### VS Code API моки - [vscodeMocks.ts](mdc:src/test/mocks/vscodeMocks.ts):
- `mockVscode` - основной объект VS Code API
- `setActiveEditor(language)` - установка активного редактора
- `setupVSCodeMocks()` / `resetVSCodeMocks()` - настройка/сброс

### Тестовые данные - [testData.ts](mdc:src/test/fixtures/testData.ts):
- `testApiResponses` - ответы OpenAI API
- `testUserSettings` - пользовательские настройки
- `testLanguageConfigs` - конфигурации языков

## ✅ Создание новых тестов

### Шаблон Unit теста:
```typescript
import * as assert from 'assert';
import * as sinon from 'sinon';
import { YourModule } from '../../path/to/YourModule';
import { setupWebAudioMocks, cleanupWebAudioMocks } from '../mocks/webAudioMocks';
import { setupVSCodeMocks, resetVSCodeMocks } from '../mocks/vscodeMocks';

suite('YourModule Unit Tests', () => {
    let yourModule: YourModule;
    let clock: sinon.SinonFakeTimers;

    setup(() => {
        setupWebAudioMocks();
        setupVSCodeMocks(); 
        clock = sinon.useFakeTimers();
        yourModule = new YourModule();
    });

    teardown(() => {
        cleanupWebAudioMocks();
        resetVSCodeMocks();
        clock.restore();
        sinon.restore();
    });

    suite('Method Tests', () => {
        test('Should do something', async () => {
            // Arrange
            const expectedResult = 'expected';
            
            // Act
            const result = await yourModule.doSomething();
            
            // Assert
            assert.strictEqual(result, expectedResult);
        });
    });
});
```

### Шаблон Integration теста:
```typescript
import * as assert from 'assert';
import * as sinon from 'sinon';
import { setupWebAudioMocks, cleanupWebAudioMocks, createMockApiResponse } from '../mocks/webAudioMocks';
import { setupVSCodeMocks, resetVSCodeMocks, setActiveEditor } from '../mocks/vscodeMocks';

suite('Integration Tests', () => {
    setup(() => {
        setupWebAudioMocks();
        setupVSCodeMocks();
    });

    teardown(() => {
        cleanupWebAudioMocks();
        resetVSCodeMocks();
        sinon.restore();
    });

    test('Should complete full workflow', async () => {
        // Настройка компонентов
        const editor = setActiveEditor('javascript');
        const fetchStub = (global as any).fetch;
        fetchStub.resolves(createMockApiResponse('Test result'));
        
        // Тестирование полного workflow
        // ...
        
        assert.ok(true);
    });
});
```

## 🎯 Существующие тесты

### Unit тесты:
- [AudioRecorder.test.ts](mdc:src/test/unit/AudioRecorder.test.ts) - 13 тестов ✅
- [WhisperClient.test.ts](mdc:src/test/unit/WhisperClient.test.ts) - 17 тестов ✅  
- [StatusBarManager.test.ts](mdc:src/test/unit/StatusBarManager.test.ts) - 10 тестов ✅
- [TextInserter.test.ts](mdc:src/test/unit/TextInserter.test.ts) - 22 теста (требует доработки)

### Integration тесты:
- [extension.integration.test.ts](mdc:src/test/integration/extension.integration.test.ts) - 12 тестов ✅

## 🔍 Рекомендации по тестированию

### Что тестировать:
1. **Основную функциональность** - запись, транскрипция, вставка текста
2. **Error handling** - ошибки API, отсутствие микрофона, неверные настройки
3. **Edge cases** - пустые данные, большие файлы, timeout'ы
4. **VS Code integration** - команды, настройки, активные редакторы
5. **Multi-language support** - разные типы файлов и комментарии

### Лучшие практики:
- Используйте `sinon.SinonFakeTimers` для timeout'ов
- Мокайте внешние API (fetch, getUserMedia)
- Тестируйте как успешные сценарии, так и ошибки
- Проверяйте вызовы функций через `sinon.SinonStub`
- Стремитесь к покрытию 80%+ (настроено в nyc)

### Отладка тестов:
```bash
# Запуск конкретного теста
npx mocha out/test/unit/AudioRecorder.test.js

# Запуск с подробным выводом
npx mocha out/test/unit/AudioRecorder.test.js --reporter=tap

# Компиляция без запуска
npm run compile-tests
```

## 📊 Покрытие кода

Цели покрытия (настроены в [package.json](mdc:package.json)):
- **Lines**: 80%
- **Statements**: 80%  
- **Functions**: 80%
- **Branches**: 70%

Отчеты генерируются в `coverage/` директории после `npm run test:coverage`.
